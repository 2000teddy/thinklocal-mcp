# changes/2026-07-24 — docs(reconcile): PR-Nummern-Nachtrag für #327

**Typ:** **Doc-only** Post-Merge-Reconcile (kein Code/Test/Design). Der Selbst-Reconcile, den **#327** beim
Schreiben nicht falten konnte — eine PR kennt ihre eigene Merge-Nummer noch nicht. Kein Deploy/Secret/Host.

## Befund
`#327` (TL-12 Zustellpfad end-to-end) ist seit **2026-07-23 14:43 UTC** gemergt (mergeCommit `f5677e9`).
Weil sie ihre eigene Nummer beim Verfassen nicht kannte, trug ihre Bookkeeping-Zeile noch den
Vor-Merge-Zustand:
- `COMPLIANCE-TABLE.md`: Erst-Spalte `(offen, base=main)` statt `#327`;
- `CHANGES.md`: Überschrift ohne `#327`-Marker;
- `TODO.md`: Eintrag ohne PR-Ref.

(#327 selbst hatte den **#326**-Reconcile bereits mit-gefaltet — nur die eigene Nummer blieb offen.)

## Was
3 Annotationen, rein 1:1 in-place:
- COMPLIANCE-Erst-Spalte → `#327` + `(base=main, gemergt)` (9→10 Spalten wie #288–#326);
- CHANGES-Überschrift um `, #327)` ergänzt;
- TODO-Eintrag `Zustellpfad end-to-end dokumentiert` annotiert.

## Verifikation
- `gh pr view 327` → `state=MERGED`, `mergedAt=2026-07-23T14:43:19Z`, `mergeCommit=f5677e9`, Titel-/Topic-Match.
- Timestamp-Anker `2026-07-23 16:45` vor der Ersetzung eindeutig.
- Nach dem Reconcile **0** stale `(offen, base=main)`-Erst-Spalten mit PR-Bezug außer der Zeile dieses PRs.
- Kein `.ts`-Diff; Suite unverändert **2045 grün** (143 Files).

## Compliance
- **CO/CG/TS:** entfallen — Doc-only-Reconcile (Präzedenz #313/#318/#321), kein Code/Test/Design.
- **CR:** externes Review am PR + Self-CR (Zuordnung gegen den gemergten PR-Titel/-Timestamp gegengeprüft).
- **PC:** Secret-Scan clean (nur Doku).
- **DO:** dieser Eintrag, `COMPLIANCE-TABLE.md`, `CHANGES.md`, `TODO.md`.
