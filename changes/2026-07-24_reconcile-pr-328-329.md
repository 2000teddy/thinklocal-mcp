# changes/2026-07-24 — docs(reconcile): PR-Nummern-Nachtrag für #328 + #329

**Typ:** **Doc-only** Post-Merge-Reconcile. Kein Code/Test/Design, kein Gate verschoben, kein
Deploy/Secret/Host. Präzedenz: #313/#318/#321/#328 (dieselbe Erst-Spalten-Selbst-Reconcile-Lücke).

## Die Lücke
Eine PR kennt ihre eigene Merge-Nummer beim Schreiben noch nicht ⇒ die COMPLIANCE-Erst-Spalte bleibt beim
Merge auf `(offen, base=main)`, die CHANGES-Überschrift ohne eigenen `#NNN`-Marker. Nach dem Merge muss ein
Folge-PR das nachziehen.

**Kontext:** Der ursprünglich erbetene #327-Nachtrag war **bereits erledigt** — #328 (`PR-Nummern-Nachtrag
für #327`) hatte #327s COMPLIANCE-Zeile (`#327` + `(base=main, gemergt)`, `COMPLIANCE-TABLE.md`) und
CHANGES-Eintrag (`…, #327)`) gesetzt. Offen war die **nächste** Stufe: #328 und #329 selbst. Ein erneutes
#327-Nachtragen hätte nur Duplikate erzeugt.

## Was (1:1 in-place, `gh`-verifiziert)
- **#328** — `PR-Nummern-Nachtrag für #327`, `gh pr view 328` → `MERGED`, `mergedAt=2026-07-24T09:09:25Z`,
  `e06ce3c`:
  - COMPLIANCE-Erst-Spalte `(offen, base=main)` → `#328` + `(base=main, gemergt)`.
  - CHANGES-Überschrift `(2026-07-24 11:05)` → `(2026-07-24 11:05, #328)`.
- **#329** — `TL-11-Runbook optionaler Daemon-Sweep`, `gh pr view 329` → `MERGED`,
  `mergedAt=2026-07-24T14:08:34Z`, `d61174f`:
  - COMPLIANCE-Erst-Spalte `(offen, base=main)` → `#329` + `(base=main, gemergt)`.
  - CHANGES-Überschrift `(2026-07-24 16:05)` → `(2026-07-24 16:05, #329)`.

Nach diesem Reconcile: **0** stale `(offen, base=main)`-Erst-Spalten mit PR-Bezug außer der Erst-Spalte
**dieses** PRs (die dieselbe Selbst-Reconcile-Lücke hat und im nächsten Nachtrag fällt).

## Compliance
- **CO/CG/TS:** entfallen — kein Code/Test/Design-Diff, reine Doku-Hygiene; `clink`/`gemini` nicht im PATH.
  Suite unverändert **2045 grün** (143 Files; kein `.ts`-Diff).
- **CR:** externes Review am PR (Bot-Pfad) + Self-CR (nur Erst-Spalten-Split + Überschriften-Marker, gegen
  `gh pr view` abgeglichen).
- **PC:** Secret-Scan clean (nur Doku).
- **DO ✅:** dieser Eintrag, `COMPLIANCE-TABLE.md`, `CHANGES.md`.

**Unverändert gated/liegen gelassen:** ADR-046 §9, msg 1453 (Ball bei Christian) — **nicht** berührt.
Risiko-Delta **null**.
