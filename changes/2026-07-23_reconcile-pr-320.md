# changes/2026-07-23 — docs(reconcile): PR-Nummern-Nachtrag für #320

**Typ:** **Doc-only** Post-Merge-Reconcile (kein Code/Test/Design). Der nach dem Merge fällige
Nummer-Nachtrag für **#320** — dieselbe 1:1-in-place-Annotation wie #313/#318. Kein Deploy/Secret/Host.

## Befund
`#320` (test(tl11): Emitter-Ende-zu-Ende-Conformance) ist seit **2026-07-23 11:07 UTC** gemergt
(mergeCommit `f47edea`), seine Bookkeeping-Einträge trugen aber noch den Vor-Merge-Zustand:

| Datei | vorher | nachher |
|---|---|---|
| `COMPLIANCE-TABLE.md` | Erst-Spalte `(offen, base=main)` (9 Spalten) | `#320` + `(base=main, gemergt)` (10 Spalten, Format wie #288–#319) |
| `CHANGES.md` | Überschrift ohne Nummer | `(2026-07-23 12:45, #320)` |
| `TODO.md` | `**TL-11 Emitter-Ende-zu-Ende-Conformance** (2026-07-23)` | `(2026-07-23, #320)` |

## Verifikation
- **Merge-Zustand live:** `gh pr view 320` → `state=MERGED`, `mergedAt=2026-07-23T11:07:16Z`,
  `mergeCommit=f47edea`; Titel-/Topic-Match gegen die annotierte Zeile geprüft.
- Der Timestamp-Anker `2026-07-23 12:45` war vor der Ersetzung **eindeutig** (genau 1 Treffer).
- Nach dem Reconcile: **0** Zeilen mit `(offen, base=main)` in der **Erst-Spalte** außer der Zeile dieses
  PRs. (Die 31 verbleibenden Vorkommen sind der in #318 §benannte **Vor-#271-Rest** — Zeilen, deren
  Erst-Spalte eine Version/ein Label statt einer PR-Nummer trägt — plus Prosa-Zitate in Reconcile-Zeilen.
  Unverändert bewusst offen: Version→PR ist Archäologie, gehört in einen eigenen PR.)
- Diff rein 1:1 in-place (3 Annotationen) + additive Selbst-Doku; keine Inhalts-/Struktur-/Verdikt-Änderung.
- Suite unverändert **1979 grün** (140 Files) — kein `.ts`-Diff.

## Compliance
- **CO/CG/TS:** entfallen — Doc-only-Reconcile (Präzedenz #84/#286/#296/#313/#318), kein Code/Test/Design.
  Verifikation über die CI-Checks am PR (inkl. Ebene-1 `Doc-Compliance-Gate`).
- **CR:** externes Review am PR + Self-CR (Zuordnung gegen den gemergten PR-Titel/-Timestamp gegengeprüft,
  keine erfundene Nummer).
- **PC:** Secret-Scan clean (nur Doku).
- **DO:** dieser Eintrag, `CHANGES.md`, `COMPLIANCE-TABLE.md`, `TODO.md`.
