# changes/2026-07-28 — docs(reconcile): PR-Nummern-Nachtrag für #346 + #347

**Typ:** **Doc-only** Post-Merge-Reconcile. Kein Laufzeitcode, kein Test-Diff, kein Gate verschoben, kein
Deploy/Secret/Host. Präzedenz: #328/#330/#332/#336/#339/#340/#345 (dieselbe Erst-Spalten-Selbst-Reconcile-Lücke).

## Die Lücke
Eine PR kennt ihre eigene Merge-Nummer beim Schreiben noch nicht ⇒ die COMPLIANCE-Erst-Spalte bleibt beim
Merge auf `(offen, base=main)`, die CHANGES-Überschrift ohne eigenen `#NNN`-Marker. Nach der Serie bis #345
blieben die beiden jüngsten Merges offen: **#346** (der #341–#345-Reconcile selbst) und **#347**
(ADR-045-Klassifikations-Hinweis). Zusätzlich hing `TODO.md` bei #338 zurück, während COMPLIANCE/CHANGES
schon #341–#345 trugen — Doku-Kanon driftete gegeneinander.

## Was (1:1 in-place, `gh`-verifiziert MERGED)
| PR | Titel | mergedAt | Commit |
|---|---|---|---|
| **#346** | docs(reconcile): PR-Nummern-Nachtrag für #341 … #345 | 2026-07-27T07:45:05Z | `923142f` |
| **#347** | docs(tl14a): ADR-045-Klassifikations-Hinweis zu Auflage C (Consensus vs. §100) | 2026-07-27T08:45:12Z | `0345546` |

- **COMPLIANCE-TABLE.md:** je Zeile Erst-Spalte `(offen, base=main)` → `#NNN` + `(base=main, gemergt)`
  (08:42 → #346, 09:45 → #347). Danach **0** stale `(offen, base=main)`-Erst-Spalten mit PR-Bezug außer der
  Erst-Spalte **dieses** PRs (dieselbe Selbst-Reconcile-Lücke, fällt im nächsten Nachtrag).
- **CHANGES.md:** beide Überschriften um `, #NNN)` ergänzt (08:42 → #346, 09:45 → #347).
- **TODO.md (Aufholung #338 → #347):** die substantiellen Task-Zeilen der Merges seit #338 trugen Datum aber
  **keine** Merge-Nummer — nachgezogen (Datum → Datum + `#NNN`): **#341** (C-Revocation-Grounding) · **#342**
  (A-Transport-pathLen) · **#343** (tl12 B1-Reihenfolge) · **#344** (B-Monitor-Verdrahtung) · **#347**
  (ADR-045-Klassifikations-Hinweis). Die reinen Reconcile-PRs **#340/#345/#346** haben keinen TODO-Task-Eintrag.
  Damit spiegeln COMPLIANCE/CHANGES/TODO dieselben Nummern bis #347.

## Compliance
- **CO/CG/TS:** entfallen — kein Code/Test/Design-Diff, reine Doku-Hygiene; `clink`/`gemini` nicht im PATH.
  Suite unverändert **2101 grün** (kein `.ts`-Diff).
- **CR:** Review-of-Record über **agy** (Gemini 3.1 Pro — zulässig; **nie** MiniMax/pal:chat) + Self-CR.
- **PC:** Secret-Scan clean (nur Doku).
- **DO ✅:** dieser Eintrag, `COMPLIANCE-TABLE.md`, `CHANGES.md`, `TODO.md`.

**Nicht berührt:** ADR-046 §9, msg 1453, alle offenen Gates (TL-08-Gate-Flip, TL-09-Aktivierung, TL-14a-C-Entscheidung/D3/Runbook/TL-14b). Risiko-Delta **null**.
