# changes/2026-07-25 — docs(reconcile): PR-Nummern-Nachtrag für #330 + #331

**Typ:** **Doc-only** Post-Merge-Reconcile. Kein Code/Test/Design, kein Gate verschoben, kein
Deploy/Secret/Host. Präzedenz: #313/#318/#321/#328/#330 (dieselbe Erst-Spalten-Selbst-Reconcile-Lücke).

## Die Lücke
Eine PR kennt ihre eigene Merge-Nummer beim Schreiben noch nicht ⇒ die COMPLIANCE-Erst-Spalte bleibt beim
Merge auf `(offen, base=main)`, die CHANGES-Überschrift ohne eigenen `#NNN`-Marker. Ein Folge-PR zieht das
nach.

**Kontext:** Der ggf. vermutete **#328**-Nachtrag war **bereits durch #330 erledigt** (Zeile 1973 trägt
`#328`, CHANGES-Überschrift `…, #328)`). Offen waren die **beiden nächsten** Zeilen: **#330** und **#331**.

## Was (1:1 in-place, `gh`-verifiziert)
- **#330** — `PR-Nummern-Nachtrag für #328 + #329`, `gh pr view 330` → `MERGED`,
  `mergedAt=2026-07-24T16:33:17Z`, `f58b8d1`:
  - COMPLIANCE-Erst-Spalte `(offen, base=main)` → `#330` + `(base=main, gemergt)`.
  - CHANGES-Überschrift `(2026-07-24 18:16)` → `(2026-07-24 18:16, #330)`.
- **#331** — `feat(tl11): konsumentenseitiger Wake-Kern interpretWakeFrame`, `gh pr view 331` → `MERGED`,
  `mergedAt=2026-07-25T04:17:37Z`, `340b7c2`:
  - COMPLIANCE-Erst-Spalte `(offen, base=main)` → `#331` + `(base=main, gemergt)`.
  - CHANGES-Überschrift `(2026-07-25 06:05)` → `(2026-07-25 06:05, #331)`.

Nach diesem Reconcile: **0** stale `(offen, base=main)`-Erst-Spalten mit PR-Bezug außer der Erst-Spalte
**dieses** PRs (dieselbe Selbst-Reconcile-Lücke, fällt im nächsten Nachtrag).

## Compliance
- **CO/CG/TS:** entfallen — kein Code/Test/Design-Diff, reine Doku-Hygiene; `clink`/`gemini` nicht im PATH.
  Suite unverändert **2065 grün** (144 Files; kein `.ts`-Diff).
- **CR:** externes Review am PR (Bot-Pfad) + Self-CR (nur Erst-Spalten-Split + Überschriften-Marker, gegen
  `gh pr view` abgeglichen).
- **PC:** Secret-Scan clean (nur Doku).
- **DO ✅:** dieser Eintrag, `COMPLIANCE-TABLE.md`, `CHANGES.md`.

**Unverändert gated/liegen gelassen:** ADR-046 §9, msg 1453 (Ball bei Christian), TL-12-Gates — **nicht**
berührt. Risiko-Delta **null**.
